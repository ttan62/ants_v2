
<% content_for(:title) {"The only notebook you'll ever need" } %>

<% content_for :content do %>
<% end %>

<div class="container">

	<h1><%= @project.name %>: Notes</h1><button type="button" class="btn"><%= link_to 'Back to Projects', projects_path %></button>

	<br /><br />

	<button type="button" class="btn btn-success"><%= link_to 'New Note', new_project_note_path(@project) %></button>

	<br /><br />
	
	<div class="container">

		<h1>Project / <%= @project.name %></h1>

        <%= form_tag project_notes_path, :method => 'get' do %>
            <p>
                <%= text_field_tag :search, params[:search] %>
                <%= submit_tag "Search", :name => nil %>
            </p>
        <% end %>
	
		<br />

		<table>
		  <tr>
		    <th width="15%">Name</th>
		    <th width="25%">Description</th>
		    <th width="10%">User</th>
		    <th width="5%">Project</th>
		    <th width="5%">Note</th>
		    <th width="25%">Content</th>
		    <th></th>
		    <th></th>
		    <th></th>
		  </tr>

		<% @notes.each do |note| %>
		  <tr>
		    <td><%= note.name %></td>
		    <td><%= note.description %></td>
		    <td><%= note.user %></td>
		    <td><%= note.project_id %></td>
		    <td><%= note.note_id %></td>
		    <td><%= note.content %></td>
		<!--    <td><button><%= link_to 'Show', project_note_path(note.project, note) %></button></td>
		    <td><button><%= link_to 'Edit', edit_project_note_path(note.project, note) %></button></td>
		    <td><button><%= link_to 'Destroy', project_note_path(note.project, note), method: :delete, data: { confirm: 'Are you sure?' } %></button></td> !-->
		    <td><%= link_to 'Show', project_note_path(note.project, note) %></td>
		    <td><%= link_to 'Edit', edit_project_note_path(note.project, note) %></td>
		    <td><%= link_to 'Destroy', project_note_path(note.project, note), method: :delete, data: { confirm: 'Are you sure?' } %></td>
		  </tr>
		<% end %>
		</table>

		<br />

		<%= link_to 'New Note', new_project_note_path(@project) %>

		<br /><br /><br /><br /><br />
		
		<h2>For SciAnts!</h2>
		<a href="/pages/home">Demo Home</a> | Created at <a href="http://angelhack.com/">AngelHack Summer 2012</a>
		<h3>Powered by </h3><%= image_tag "evernote.png", :width => "250px", alt: "Evernote" %>
>>>>>>> d15bd3896f98e52c8b03e8493b2c61ab7057aed1

	<% @notes.each do |note| %>

	<div class="notes">
		<h3><%= note.name %></h3>
		<p><%= note.description %></p>
		<p><%= note.created_at %></p>
		<p><%= note.project_id %></p>
		<p><%= note.note_id %></p>
		<p><%= note.content %></p>

		<button type="button" class="btn"><%= link_to 'Open', edit_project_note_path(note.project, note) %></button><button type="button" class="btn btn-danger"><%= link_to 'Destroy', project_note_path(note.project, note), method: :delete, data: { confirm: 'Are you sure?' } %></button>

	</div>
	
	<% end %>
</div>
